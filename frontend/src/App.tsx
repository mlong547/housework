function App() {
  return (
    <main className="app-shell" aria-labelledby="app-title">
      <section className="app-shell__panel">
        <p className="app-shell__eyebrow">Housework</p>
        <h1 id="app-title">Frontend foundation</h1>
        <p>
          This placeholder confirms the React, TypeScript, test, lint, and
          design-token setup before app features are added.
        </p>
      </section>
    </main>
  )
}

export default App
