# Housework Frontend

React and TypeScript frontend for Housework.

## Scripts

```sh
yarn install
yarn dev
yarn lint
yarn test
yarn test:ci
yarn build
```

Use Yarn Classic. The expected package manager is recorded in `package.json`.
